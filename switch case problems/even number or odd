#include<stdio.h>
int main()
{
    int n;
    printf("enter number :");
    scanf("%d",&n);
    switch(n%2)
    {
        case 0:
               printf("the number is even");
               break;
        default:
               printf("the number is odd");
    }
    return 0;
}
