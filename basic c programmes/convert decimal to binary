#include<stdio.h>
int main()
{
    int n,rem,x=0;
    printf("enter the number :");
    scanf("%d",&n);
    while(n>0)
    {
        rem=n%2;
        x=rem;
        n=n/2;
        printf("%d",x);
    }
}
