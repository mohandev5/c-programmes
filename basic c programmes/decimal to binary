#include <stdio.h>
int main()
{
    int base=1,d=0,n,r=0;
    printf("enter a decimal number:");
    scanf("%d",&n);
    while(n>0)
    {
        r=n%2;
        d=d+r*base;
       n=n/2;
        base=base*10;
    }
    printf("binary no :%d",d);
    return 0;
}

