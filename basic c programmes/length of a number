#include<stdio.h>
int main()
{
    int n,c=0,rem;
    printf("enter the number :");
    scanf("%d",&n);
    while(n!=0)
    {
        rem=n%10;
        c=c+1;
        n=n/10;
    }
    printf("length of a number :%d",c);
}
