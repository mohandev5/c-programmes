#include <stdio.h>
int main()
{
    int base=1,d=0,n,r=0;
    printf("enter binary number:");
    scanf("%d",&n);
    while(n>0)
    {
        r=n%10;
        d=d+r*base;
       n=n/10;
        base=base*2;
    }
    printf("decimal number :%d",d);
    return 0;
}

