#include <stdio.h>
int main() 
{
   char ch;
   printf("enter a character:");
   scanf("%c",&ch);
   if(ch=='z'||ch=='Z')
   {
       printf("%c",97);
   }
else
   {
       printf("%c",ch+1);
   }
    return 0;
}
